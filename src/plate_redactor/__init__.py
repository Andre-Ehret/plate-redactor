"""plate-redactor — on-device licence-plate detector for privacy redaction.

Phase 0 only bootstraps the package; the synthetic data generator (Phase 1),
training (Phase 2), evaluation (Phase 3) and TFLite export (Phase 4) land in
later phases. See README.md for the interface contract with the consuming app.
"""

__version__ = "0.0.0"
